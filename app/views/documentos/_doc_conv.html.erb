

<div class="flex justify-center items-center mt-10">
   <div>
        <div class="font-semibold">Convenio o algún otro documento:</div>
              <% if @proyecto.docconvenios.attached? %>
                     <% @proyecto.docconvenios.each do |doc| %>
                            <div class="ml-4"> <%= link_to doc.filename, rails_blob_path(doc, disposition: 'inline'), target: :_blank %></div>                
                     <% end %>
              <% else %>                     
                     <div class="font-semibold text-gray-600 italic ml-4"> El proyecto no incluye este tipo de documentos.</div>
              <% end %>
       </div>
</div>
