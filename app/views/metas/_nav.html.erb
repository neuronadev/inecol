<% 
   puts 'Paso module:' + Nav::stepAct.to_s
   puts 'Proyecto: ' + session[:proyecto_id].to_s

   puts 'Paso session:' + session[:step].to_s
%>
<%  if session[:step] == 5 %>
      <%= link_to 'Atras', recurso_path(@proyecto.recurso.id) %>
<% end %>
<%  if session[:step] == 6 %>
      <%= link_to 'Atras', recurso_path(@meta.proyecto.recurso.id) %>
<% end %>