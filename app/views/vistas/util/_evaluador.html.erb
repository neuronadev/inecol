<div class="flex mt-5 mb-1 border-b-2 border-slate-200 p-2">
      <div>
            <%
                validacion = @proyecto.validaciones.includes(:evaluador).where('evaluadores.persona_id':current_usuario.cuenta.persona.id).last
            %>
            <% if validacion.nil? %>               
                  <div>
                        <%= link_to new_validacion_path(:idpy=>@proyecto.id, :tval=>Tvalidacion.where(clave:'ACEP').first.id), 'data-frame-target':'_top' do %>
                        <div class="flex hover:cursor-pointer font-medium text-sm leading-tight ml-3" >
                              <div> <%= inline_svg_tag 'svg/usuarios-alt.svg' %></div> <div class="ml-2 text-sky-800">Validar proyecto</div>
                        </div>     
                        <% end %>    
                  </div>  
             <% else %>   
                  <div>
                    <%= link_to validacion_path(validacion.id), 'data-frame-target':'_top' do %>
                      <div class="flex hover:cursor-pointer font-medium text-sm leading-tight ml-3" >
                            <div> <%= inline_svg_tag 'svg/usuarios-alt.svg' %></div> <div class="ml-2 text-sky-800">Ver validación</div>
                      </div>     
                     <% end %>    
                </div>   
            <% end %>    
       </div>     
       
<div>