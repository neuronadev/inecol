<div class="w-full">
    <div class="mt-3 flex align-middle border-b-2 border-slate-200">
      <div><%= inline_svg_tag 'svg/listaproy' %></div>
      <div class="ml-3 text-slate-700">Listado de Proyectos Registrados</div>
    </div>
  
     <% if current_usuario.email == 'sara.sanchez@inecol.mx' %>
             <%= render 'index_enlace' %>  
     <% else %> 
             <%= render 'index' %>  
     <% end %>

</div>    