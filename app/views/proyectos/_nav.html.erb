<% 
   puts 'Paso module:' + Nav::stepAct.to_s
   puts 'Paso session:' + session[:step].to_s

%>

<%  if session[:step] == 1 %>
  <%= link_to 'Siguiente', new_fuente_path %>
<% end %>

<%  if session[:step] == 2 || session[:step] == 3 || session[:step] == 4 || session[:step] == 5  %>
  <%= link_to 'Siguiente', fuente_path(@proyecto.fuente.id) %>
<% end %>

