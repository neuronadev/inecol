<style>
th {
  background-color: rgb(229 231 235 / var(--tw-bg-opacity));
  position: -webkit-sticky;
  position: sticky;
  top: 0;
  z-index: 2;
}
</style>
<div class="relative overflow-x-auto shadow-md sm:rounded-lg mt-5" style="height: 80vh;">
    <table class="w-full text-sm text-left text-gray-500 dark:text-gray-400">
        <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
            <tr>
                <th scope="col" class="p-1 text-center">
                    Num. de Registro
                </th> 
                <th scope="col" class="px-6 py-3 text-center" style="width:50%;">
                    Nombre del Proyecto
                </th>
                <th scope="col" class="px-6 py-3 text-center">
                    Fecha de Registro
                </th>

                <th scope="col" class="px-6 py-3 text-center">
                    Responsable
                </th>

                <th scope="col" class="px-6 py-3 text-center" style="width:10%;">
                    Estado
                </th>
               
                <th scope="col" class="px-6 py-3 text-center">
                    Acciones
                </th>
            </tr>
        </thead>
        <tbody>
               <%  @validaciones.each do |item| %>
                     <%= render 'proyectos/rwdata', py:item, tipo:'V' %>
               <% end %>
               <%  @validado.each do |item| %>
                     <%= render 'proyectos/rwdata', py:item, tipo:'' %>
               <% end %>

        </tbody>
    </table>
</div>